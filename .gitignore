__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
tmp/
.venv/
venv/
.workbuddy/
battle/

# Keep only precedent indexes/pattern cards in Git. Store paper files locally.
references/precedents/local-sources/
references/precedents/**/*.pdf
references/precedents/**/*.doc
references/precedents/**/*.docx
references/precedents/**/*.zip
references/precedents/**/*.rar
*.egg-info/
build/
dist-test/
.env
.DS_Store
Thumbs.db

# Local shallow clones used for template compatibility and license audits.
# They are upstream working copies, not distributable Skill payload.
vendor/upstream/

# Generated by CI from the red-team evaluation; regenerate, do not commit.
evaluation/results/

# Agent session state for this workspace; may contain private context.
.qoder/

# Recovery benchmark baseline is built per run (see build_slim_fixture.py).
evaluation/recovery/fixtures/slim_v2/
