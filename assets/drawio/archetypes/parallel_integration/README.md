# Parallel integration

Grammar: aligned input/model lanes → one topology-derived merge hub → explicit final strategy. Color is secondary to lane and convergence geometry.

Reference mechanism: semantic grouping and one primary reading order; the planner never invents fusion edges.

