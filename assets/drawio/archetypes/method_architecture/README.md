# Method architecture

Grammar: emphasized optimization core at the center; inputs, objective, and constraints feed it; output and validation have distinct tiers.

Reference mechanism: central-container composition inspired by method/agent architecture figures, with all platform semantics removed.

