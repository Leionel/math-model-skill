# Computational pipeline

Grammar: continuous left-to-right execution chain with a larger core model and compact input/evaluation/output blocks. Side inputs may stack within a layer without changing the DAG.

Reference mechanism: Model Pipeline continuity plus compact scientific labels; no software-platform semantics are retained.

